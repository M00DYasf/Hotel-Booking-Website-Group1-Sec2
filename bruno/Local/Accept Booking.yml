info:
  name: Accept Booking
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/api/bookings/accept/123"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
