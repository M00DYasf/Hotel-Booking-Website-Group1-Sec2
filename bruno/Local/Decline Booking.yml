info:
  name: Decline Booking
  type: http
  seq: 5

http:
  method: PUT
  url: "{{baseUrl}}/api/bookings/decline/123"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
